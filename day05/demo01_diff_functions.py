"""
    four kinds of function:
        1. Non-parametrized, no return values
            usually use to print out information
        2. Non-parameterized, with return values
            usually use to collect information
                def myLaptop():
                    # collect laptop's specs.
        3. Parameterized, no return values
            usually use in the parameters which don't need return values.
                def setPara(x):
                    pass
        4. Parameterized, with return values
            usually use to calculate, then return result of the calculation.
                def calc(x,y)
                    z = x + y
                    return z
"""
